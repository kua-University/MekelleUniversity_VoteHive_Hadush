<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.musr_votehive">

    <!-- Add necessary permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MUSR VoteHive"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Material3.DayNight">

        <!-- Declare activities here -->
        <activity
            android:name=".AddAdminActivity"
            android:exported="false" />
        <activity
            android:name=".AddCandidateActivity"
            android:exported="false" />
        <activity
            android:name=".ChangeAdminPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".AdminLoginActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true" /> <!-- Fixed exported attribute -->

        <activity android:name=".RegisterActivity" />
        <activity android:name=".StudentDashboardActivity" />

        <!-- Ensure MainActivity is exported and has intent filters -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>
</manifest>





